import 'package:flutter/material.dart';

class PhoneLockScreen extends StatelessWidget {
  const PhoneLockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}